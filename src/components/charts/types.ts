export interface SalesData {
  category: string;
  sales: number;
}

export interface RevenueData {
  month: string;
  revenue: number;
}

export interface ProductData {
  name: string;
  unitsSold: number;
}
